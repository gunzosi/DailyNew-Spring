package code.mentor.models;

public enum CustomElementType {
    NAME,
    TEXT,
    ADDRESS,
    EMAIL,
    PHONE,
    NUMBER,
    DATE,
    AGE,
    KEYWORD; // Enum mới

    // Các phương thức cần thiết
}
